import { LoadingMatButtonDirective } from './loading-mat-button.directive';

describe('LoadingMatButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingMatButtonDirective();
    expect(directive).toBeTruthy();
  });
});
