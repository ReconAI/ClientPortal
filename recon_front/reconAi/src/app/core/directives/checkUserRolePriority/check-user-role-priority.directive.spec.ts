import { CheckUserRolePriorityDirective } from './check-user-role-priority.directive';

describe('CheckUserRolePriorityDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckUserRolePriorityDirective();
    expect(directive).toBeTruthy();
  });
});
